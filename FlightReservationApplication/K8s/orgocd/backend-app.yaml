apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: flight-reservation-backend
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/yash-deshpande24/Flight-reservation-project-Devops.git
    targetRevision: main
    path: FlightReservationApplication/k8s/backend

  destination:
    server: https://kubernetes.default.svc
    namespace: flight-reservation

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
